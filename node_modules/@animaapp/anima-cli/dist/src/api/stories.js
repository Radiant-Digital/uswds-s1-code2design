"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getTeamProcessingStories = exports.getTeamStories = void 0;
const node_fetch_1 = __importDefault(require("node-fetch"));
const node_1 = require("@sentry/node");
const constants_1 = require("../constants");
const getTeamStories = async (token) => {
    const traceHeader = (0, node_1.getCurrentHub)().getScope()?.getSpan()?.toTraceparent();
    const headers = {
        'Content-Type': 'application/json',
        Authorization: 'Bearer ' + token,
    };
    if (traceHeader) {
        headers['sentry-trace'] = traceHeader;
    }
    const res = await (0, node_fetch_1.default)(`${constants_1.STORYBOOK_SERVICE_BASE_URL}/stories`, {
        method: 'GET',
        headers,
    });
    const { results: stories } = await res.json();
    return stories;
};
exports.getTeamStories = getTeamStories;
const getTeamProcessingStories = async (token) => {
    const traceHeader = (0, node_1.getCurrentHub)().getScope()?.getSpan()?.toTraceparent();
    const headers = {
        'Content-Type': 'application/json',
        Authorization: 'Bearer ' + token,
    };
    if (traceHeader) {
        headers['sentry-trace'] = traceHeader;
    }
    return (0, node_fetch_1.default)(`${constants_1.STORYBOOK_SERVICE_BASE_URL}/stories_processing`, {
        method: 'GET',
        headers,
    });
};
exports.getTeamProcessingStories = getTeamProcessingStories;
