"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.generateZipHash = exports.zipDir = void 0;
const archiver_1 = __importDefault(require("archiver"));
const stream_1 = require("stream");
const hash_1 = require("./hash");
const debug_1 = require("./debug");
const node_1 = require("@sentry/node");
function zipDir(dir) {
    return new Promise((resolve, reject) => {
        const buffs = [];
        const converter = new stream_1.Writable();
        converter._write = (chunk, _encoding, cb) => {
            buffs.push(chunk);
            process.nextTick(cb);
        };
        converter.on('finish', () => {
            resolve(Buffer.concat(buffs));
        });
        const archive = (0, archiver_1.default)('zip', {
            zlib: { level: 9 },
        });
        archive.on('error', (err) => {
            reject(err);
        });
        archive.pipe(converter);
        archive.directory(dir, false);
        archive.finalize();
    });
}
exports.zipDir = zipDir;
const generateZipHash = async (buildDir) => {
    const transaction = (0, node_1.getCurrentHub)().getScope()?.getTransaction();
    const spanZipBuild = transaction?.startChild({ op: 'zip-build-and-hash' });
    // zip the build directory and create a hash
    const zipBuffer = await zipDir(buildDir);
    const zipHash = (0, hash_1.hashBuffer)(zipBuffer);
    (0, debug_1.isDebug)() && console.log('generated hash =>', zipHash);
    spanZipBuild?.finish();
    return { zipHash, zipBuffer };
};
exports.generateZipHash = generateZipHash;
