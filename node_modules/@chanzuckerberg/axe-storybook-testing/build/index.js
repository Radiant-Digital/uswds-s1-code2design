"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.run = void 0;
const Options_1 = require("./Options");
const Server_1 = require("./Server");
const Suite_1 = require("./Suite");
/**
 * Run the accessibility tests and return a promise that is resolved or rejected based on whether
 * any violations were detected.
 */
function run() {
    return __awaiter(this, void 0, void 0, function* () {
        const options = (0, Options_1.parseOptions)();
        const numFailed = yield (0, Server_1.runWithServer)(options, (storybookUrl) => {
            return (0, Suite_1.runSuite)(storybookUrl, options);
        });
        return new Promise((resolve, reject) => {
            return numFailed > 0 ? reject() : resolve();
        });
    });
}
exports.run = run;
